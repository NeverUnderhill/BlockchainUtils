Run ```./getTransactionCount.sh <BLOCK_NUM>``` to get number of transactions in block ```BLOCK_NUM```.

Run ```./tcrange.sh``` to run the aboce script in a loop.

Edit the script to change loop boundaries and increment.